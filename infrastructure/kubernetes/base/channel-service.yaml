apiVersion: apps/v1
kind: Deployment
metadata:
  name: channel-service
  namespace: servicedesk
  labels:
    app: channel-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: channel-service
  template:
    metadata:
      labels:
        app: channel-service
    spec:
      containers:
        - name: channel-service
          image: ghcr.io/servicedesk/channel-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8082
              name: http
          envFrom:
            - configMapRef:
                name: servicedesk-config
            - secretRef:
                name: servicedesk-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: channel-service
  namespace: servicedesk
spec:
  selector:
    app: channel-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
