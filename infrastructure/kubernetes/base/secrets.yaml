apiVersion: v1
kind: Secret
metadata:
  name: servicedesk-secrets
  namespace: servicedesk
type: Opaque
stringData:
  # Database password - CHANGE IN PRODUCTION
  DB_PASSWORD: "servicedesk"

  # JWT Secret - MUST BE CHANGED IN PRODUCTION
  JWT_SECRET: "your-256-bit-secret-key-here-for-jwt-signing-change-me"

  # RabbitMQ credentials
  RABBITMQ_USERNAME: "servicedesk"
  RABBITMQ_PASSWORD: "servicedesk"

  # MinIO credentials
  MINIO_ACCESS_KEY: "servicedesk"
  MINIO_SECRET_KEY: "servicedesk123"

  # Redis password (optional)
  REDIS_PASSWORD: ""

  # Email credentials (optional)
  MAIL_USERNAME: ""
  MAIL_PASSWORD: ""

  # AI API Keys (optional)
  OPENAI_API_KEY: ""
  ANTHROPIC_API_KEY: ""
